apiVersion: batch/v1
kind: Job
metadata:
  name: job
  namespace: default
spec:
  template:
    spec:
      containers:
      - args:
        - echo
        - hello
        image: busybox
        name: job
      restartPolicy: Never

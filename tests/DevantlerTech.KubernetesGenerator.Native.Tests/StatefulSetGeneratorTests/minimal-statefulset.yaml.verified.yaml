---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: minimal-statefulset
spec:
  selector:
    matchLabels:
      app: minimal-app
  serviceName: minimal-service
  template:
    metadata:
      labels:
        app: minimal-app
    spec:
      containers:
      - name: app
        image: busybox

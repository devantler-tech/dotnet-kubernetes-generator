---
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: github-receiver-simple
  namespace: flux-system
spec:
  events:
  - push
  resources:
  - kind: GitRepository
    name: webapp
  secretRef:
    name: github-token
  type: github
